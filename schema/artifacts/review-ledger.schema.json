{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/artifacts/review-ledger.schema.json",
  "$ref": "#/$defs/ReviewLedger",
  "$defs": {
    "ReviewLedger": {
      "properties": {
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "kind": {
          "type": "string",
          "description": "Kind is the review kind for the round."
        },
        "updated_at": {
          "type": "string",
          "description": "UpdatedAt is the timestamp of the most recent ledger update."
        },
        "assignments": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/ReviewLedgerAssignment"
              },
              "type": "array",
              "description": "Assignments lists the current state of every reviewer assignment."
            },
            {
              "type": "null"
            }
          ],
          "description": "Assignments lists the current state of every reviewer assignment."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id",
        "kind",
        "updated_at",
        "assignments"
      ],
      "description": "ReviewLedger is the command-owned ledger artifact tracking submission status for a review round."
    },
    "ReviewLedgerAssignment": {
      "properties": {
        "slot": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "description": "Status is the current submission status for the slot."
        },
        "submission_path": {
          "type": "string",
          "description": "SubmissionPath is the path where the slot submission should exist."
        },
        "submitted_at": {
          "type": "string",
          "description": "SubmittedAt is the submission timestamp when the slot has been submitted."
        },
        "aborted_at": {
          "type": "string",
          "description": "AbortedAt is the timestamp when the unfinished round was explicitly abandoned before submission."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "slot",
        "role",
        "status",
        "submission_path"
      ],
      "description": "ReviewLedgerAssignment records one reviewer assignment in the ledger."
    }
  },
  "title": "Review ledger artifact",
  "description": "Command-owned review ledger artifact for one review round."
}
