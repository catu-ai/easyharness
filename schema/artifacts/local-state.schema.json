{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/artifacts/local-state.schema.json",
  "$ref": "#/$defs/LocalStateFile",
  "$defs": {
    "FinalizeCoverageState": {
      "properties": {
        "root_round_id": {
          "type": "string"
        },
        "tip_round_id": {
          "type": "string"
        },
        "covered_head_sha": {
          "type": "string"
        },
        "revision": {
          "type": "integer"
        },
        "unresolved_blocking_count": {
          "type": "integer"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "root_round_id",
        "tip_round_id",
        "covered_head_sha",
        "revision",
        "unresolved_blocking_count"
      ],
      "description": "FinalizeCoverageState is a compact cache of the durable review coverage chain stored in review manifests and aggregates."
    },
    "LandState": {
      "properties": {
        "pr_url": {
          "type": "string",
          "description": "PRURL is the pull request URL recorded for the land phase."
        },
        "commit": {
          "type": "string",
          "description": "Commit is the merge commit or landed commit recorded for the land phase."
        },
        "landed_at": {
          "type": "string",
          "description": "LandedAt is the timestamp when the land command recorded merge completion."
        },
        "completed_at": {
          "type": "string",
          "description": "CompletedAt is the timestamp when land cleanup completed."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "LandState records the current land state in the local state file."
    },
    "LocalStateFile": {
      "properties": {
        "execution_started_at": {
          "type": "string",
          "description": "ExecutionStartedAt is the execution-start timestamp for the plan."
        },
        "revision": {
          "type": "integer",
          "description": "Revision is the current plan-local revision number."
        },
        "reopen": {
          "$ref": "#/$defs/ReopenState",
          "description": "Reopen records the active reopen repair state when one exists."
        },
        "active_review_round": {
          "$ref": "#/$defs/ReviewRoundState",
          "description": "ActiveReviewRound records the current active review round when review is in flight."
        },
        "finalize_coverage": {
          "$ref": "#/$defs/FinalizeCoverageState",
          "description": "FinalizeCoverage records the latest command-validated finalize review coverage tip. Unlike ActiveReviewRound, it survives reopen so a narrow later revision can extend the archived candidate with a linked delta."
        },
        "land": {
          "$ref": "#/$defs/LandState",
          "description": "Land records the current land state when merge cleanup is in flight."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "LocalStateFile is the plan-local runtime control artifact under the configured local runtime root's plans directory."
    },
    "ReopenState": {
      "properties": {
        "mode": {
          "type": "string",
          "description": "Mode is the active reopen mode."
        },
        "reopened_at": {
          "type": "string",
          "description": "ReopenedAt is the reopen timestamp."
        },
        "base_step_count": {
          "type": "integer",
          "description": "BaseStepCount is the number of plan steps that existed before the reopen."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "mode"
      ],
      "description": "ReopenState records the active reopen repair state."
    },
    "ReviewRoundState": {
      "properties": {
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "kind": {
          "type": "string",
          "description": "Kind is the review kind for the round."
        },
        "step": {
          "type": "integer",
          "description": "Step is the tracked plan step number when the round is step-scoped."
        },
        "revision": {
          "type": "integer",
          "description": "Revision is the plan-local revision associated with the round."
        },
        "aggregated": {
          "type": "boolean",
          "description": "Aggregated reports whether the round has already been aggregated."
        },
        "decision": {
          "type": "string",
          "description": "Decision is the aggregate review decision when one is known."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id",
        "kind",
        "aggregated"
      ],
      "description": "ReviewRoundState records the current active review round in the local state file."
    }
  },
  "title": "Plan-local state file",
  "description": "Plan-local runtime state artifact under the configured local runtime root's plans directory."
}
