{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/artifacts/review-manifest.schema.json",
  "$ref": "#/$defs/ReviewManifest",
  "$defs": {
    "ReviewAssignment": {
      "properties": {
        "slot": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "dimensions": {
          "items": {
            "$ref": "#/$defs/ReviewResolvedDimension"
          },
          "type": "array",
          "minItems": 1
        },
        "instructions": {
          "type": "string"
        },
        "risk_brief": {
          "$ref": "#/$defs/ReviewRiskBrief"
        },
        "submission_path": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "slot",
        "role",
        "dimensions",
        "instructions",
        "submission_path"
      ],
      "description": "ReviewAssignment describes one surfaced reviewer assignment."
    },
    "ReviewManifest": {
      "properties": {
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "kind": {
          "type": "string",
          "description": "Kind is the review kind for the round."
        },
        "anchor_sha": {
          "type": "string",
          "description": "AnchorSHA is the controller-chosen git commit anchor recorded for delta review when the round uses one."
        },
        "reviewed_head_sha": {
          "type": "string",
          "description": "ReviewedHeadSHA is the command-captured immutable candidate head reviewed by this round."
        },
        "step": {
          "type": "integer",
          "description": "Step is the tracked plan step number when the round is step-scoped."
        },
        "revision": {
          "type": "integer",
          "description": "Revision is the plan-local revision associated with the round."
        },
        "review_title": {
          "type": "string",
          "description": "ReviewTitle is the human-readable title for the round when one exists."
        },
        "repair": {
          "$ref": "#/$defs/ReviewRepairReference",
          "description": "Repair identifies the prior round and findings targeted by this delta."
        },
        "plan_path": {
          "type": "string",
          "description": "PlanPath is the tracked or archived plan path associated with the round."
        },
        "plan_stem": {
          "type": "string",
          "description": "PlanStem is the durable plan stem associated with the round."
        },
        "created_at": {
          "type": "string",
          "description": "CreatedAt is the round creation timestamp."
        },
        "assignments": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/ReviewAssignment"
              },
              "type": "array",
              "description": "Assignments lists the materialized reviewer assignments for the round."
            },
            {
              "type": "null"
            }
          ],
          "description": "Assignments lists the materialized reviewer assignments for the round."
        },
        "ledger_path": {
          "type": "string",
          "description": "LedgerPath is the path to the round ledger artifact."
        },
        "aggregate_path": {
          "type": "string",
          "description": "Aggregate is the path to the round aggregate artifact."
        },
        "submissions_dir": {
          "type": "string",
          "description": "Submissions is the path to the round submissions directory."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id",
        "kind",
        "reviewed_head_sha",
        "revision",
        "plan_path",
        "plan_stem",
        "created_at",
        "assignments",
        "ledger_path",
        "aggregate_path",
        "submissions_dir"
      ],
      "description": "ReviewManifest is the command-owned review manifest artifact for one review round."
    },
    "ReviewRepairReference": {
      "properties": {
        "round_id": {
          "type": "string"
        },
        "finding_ids": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 0
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id",
        "finding_ids"
      ],
      "description": "ReviewRepairReference identifies the findings targeted by a repair delta."
    },
    "ReviewResolvedDimension": {
      "properties": {
        "name": {
          "type": "string"
        },
        "sources": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "description": {
          "type": "string"
        },
        "instructions": {
          "type": "string"
        },
        "plan_path": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "sources",
        "description",
        "instructions"
      ],
      "description": "ReviewResolvedDimension snapshots one explicitly assigned guidance fragment."
    },
    "ReviewRiskBrief": {
      "properties": {
        "risk_surfaces": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "invariants": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "failure_modes": {
          "oneOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "risk_surfaces",
        "invariants"
      ],
      "description": "ReviewRiskBrief gives a specialist a concrete risk surface to challenge."
    }
  },
  "title": "Review manifest artifact",
  "description": "Command-owned review manifest artifact for one review round."
}
