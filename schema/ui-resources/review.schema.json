{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/ui-resources/review.schema.json",
  "$ref": "#/$defs/ReviewResult",
  "$defs": {
    "ErrorDetail": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path identifies the field, section, or artifact path associated with the error."
        },
        "message": {
          "type": "string",
          "description": "Message is the human-readable explanation of the problem."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "message"
      ],
      "description": "ErrorDetail describes one machine-readable validation or execution problem in a command result."
    },
    "ReviewAggregateFinding": {
      "properties": {
        "finding_id": {
          "type": "string"
        },
        "slot": {
          "type": "string",
          "description": "Slot is the stable reviewer slot identifier."
        },
        "role": {
          "type": "string"
        },
        "area": {
          "type": "string"
        },
        "severity": {
          "type": "string",
          "description": "Severity is the finding severity label."
        },
        "title": {
          "type": "string",
          "description": "Title is the short human-readable title of the finding."
        },
        "details": {
          "type": "string",
          "description": "Details is the full review finding explanation."
        },
        "locations": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Locations optionally lists lightweight repo-relative source anchors for the finding, such as \"path/to/file.go\", \"path/to/file.go#L123\", or \"path/to/file.go#L1-L3\"."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "finding_id",
        "slot",
        "role",
        "area",
        "severity",
        "title",
        "details"
      ],
      "description": "ReviewAggregateFinding is one aggregate finding annotated with its slot and dimension context."
    },
    "ReviewArtifactView": {
      "properties": {
        "label": {
          "type": "string",
          "description": "Label is the stable display label for the artifact."
        },
        "path": {
          "type": "string",
          "description": "Path is the repo-facing artifact path when one exists."
        },
        "status": {
          "type": "string",
          "description": "Status reports whether the artifact is available, missing, or invalid."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise artifact-state explanation."
        },
        "content_type": {
          "type": "string",
          "description": "ContentType reports how Content should be rendered in the UI resource."
        },
        "content": {
          "description": "Content is the resolved artifact file payload for UI tabs when readable."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "label"
      ],
      "description": "ReviewArtifactView is one raw supporting artifact view for a review round."
    },
    "ReviewArtifacts": {
      "properties": {
        "plan_path": {
          "type": "string",
          "description": "PlanPath is the current plan path associated with the review data."
        },
        "active_round_id": {
          "type": "string",
          "description": "ActiveRoundID is the currently active review round when one exists in local state."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "ReviewArtifacts points to the current plan review sources."
    },
    "ReviewAssignmentView": {
      "properties": {
        "slot": {
          "type": "string",
          "description": "Slot is the stable assignment identifier."
        },
        "role": {
          "type": "string",
          "description": "Role is integrated or specialist."
        },
        "dimensions": {
          "items": {
            "$ref": "#/$defs/ReviewResolvedDimension"
          },
          "type": "array",
          "description": "Dimensions are the snapshotted guidance fragments assigned to the reviewer."
        },
        "risk_brief": {
          "$ref": "#/$defs/ReviewRiskBrief",
          "description": "RiskBrief is present only for specialist assignments."
        },
        "instructions": {
          "type": "string",
          "description": "Instructions is the explicit reviewer handoff for this slot when it is available in surfaced review data."
        },
        "status": {
          "type": "string",
          "description": "Status is the current submission status label for the slot."
        },
        "submission_path": {
          "type": "string",
          "description": "SubmissionPath is the path to the submission artifact for this slot."
        },
        "submitted_at": {
          "type": "string",
          "description": "SubmittedAt is the submission timestamp when one is known."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the reviewer's concise overall assessment when one was submitted."
        },
        "resolutions": {
          "items": {
            "$ref": "#/$defs/ReviewFindingResolution"
          },
          "type": "array",
          "description": "Resolutions records explicit repair-finding verdicts from this reviewer."
        },
        "findings": {
          "items": {
            "$ref": "#/$defs/ReviewFinding"
          },
          "type": "array",
          "description": "Findings lists the slot-level review findings when one was submitted."
        },
        "worklog": {
          "$ref": "#/$defs/ReviewWorklogView",
          "description": "Worklog contains normalized progressive reviewer-worklog fields derived from the submission payload when available."
        },
        "raw_submission": {
          "description": "RawSubmission preserves the raw reviewer submission payload for secondary UI inspection."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-fatal degraded-state notes for this slot."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "slot",
        "role"
      ],
      "description": "ReviewAssignmentView is one reviewer-centric view of a materialized assignment plus any submission returned for it."
    },
    "ReviewFinding": {
      "properties": {
        "area": {
          "type": "string",
          "description": "Area is the actionable defect area within the reviewed candidate."
        },
        "severity": {
          "type": "string",
          "description": "Severity is the finding severity label."
        },
        "title": {
          "type": "string",
          "description": "Title is the short human-readable title of the finding."
        },
        "details": {
          "type": "string",
          "description": "Details is the full review finding explanation."
        },
        "locations": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Locations optionally lists lightweight repo-relative source anchors for the finding, such as \"path/to/file.go\", \"path/to/file.go#L123\", or \"path/to/file.go#L1-L3\"."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "area",
        "severity",
        "title",
        "details"
      ],
      "description": "ReviewFinding is one review finding in a submission or aggregate."
    },
    "ReviewFindingResolution": {
      "properties": {
        "finding_id": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "details": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "finding_id",
        "status",
        "details"
      ],
      "description": "ReviewFindingResolution records whether one referenced finding is closed."
    },
    "ReviewResolvedDimension": {
      "properties": {
        "name": {
          "type": "string"
        },
        "sources": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "description": {
          "type": "string"
        },
        "instructions": {
          "type": "string"
        },
        "plan_path": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "sources",
        "description",
        "instructions"
      ],
      "description": "ReviewResolvedDimension snapshots one explicitly assigned guidance fragment."
    },
    "ReviewResult": {
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "OK reports whether review loading succeeded."
        },
        "resource": {
          "type": "string",
          "description": "Resource is the stable UI resource identifier."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise human-readable explanation of the loaded review rounds."
        },
        "artifacts": {
          "$ref": "#/$defs/ReviewArtifacts",
          "description": "Artifacts points to the current-plan review artifact paths used to build this response."
        },
        "rounds": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/ReviewRoundView"
              },
              "type": "array",
              "description": "Rounds lists the discovered review rounds for the current plan."
            },
            {
              "type": "null"
            }
          ],
          "description": "Rounds lists the discovered review rounds for the current plan."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-fatal degraded-state notes for the overall review resource."
        },
        "errors": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Errors lists hard failures that prevented review loading."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ok",
        "resource",
        "summary",
        "rounds"
      ],
      "description": "ReviewResult is the read-only UI resource returned by `/api/review`."
    },
    "ReviewRiskBrief": {
      "properties": {
        "risk_surfaces": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "invariants": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "failure_modes": {
          "oneOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "risk_surfaces",
        "invariants"
      ],
      "description": "ReviewRiskBrief gives a specialist a concrete risk surface to challenge."
    },
    "ReviewRoundView": {
      "properties": {
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "kind": {
          "type": "string",
          "description": "Kind is the review kind for the round."
        },
        "anchor_sha": {
          "type": "string",
          "description": "AnchorSHA is the delta-review anchor commit when one is available."
        },
        "reviewed_head_sha": {
          "type": "string",
          "description": "ReviewedHeadSHA is the immutable candidate commit captured for this round."
        },
        "repairs_round_id": {
          "type": "string",
          "description": "RepairsRoundID is the direct parent coverage round for a repair delta."
        },
        "step": {
          "type": "integer",
          "description": "Step is the tracked plan step number when the round is step-scoped."
        },
        "revision": {
          "type": "integer",
          "description": "Revision is the plan-local revision associated with the round."
        },
        "review_title": {
          "type": "string",
          "description": "ReviewTitle is the human-readable title for the round when one exists."
        },
        "status": {
          "type": "string",
          "description": "Status is the stable UI status label for the round."
        },
        "status_summary": {
          "type": "string",
          "description": "StatusSummary is the concise explanation of the current round status."
        },
        "decision": {
          "type": "string",
          "description": "Decision is the aggregate decision when one is known."
        },
        "created_at": {
          "type": "string",
          "description": "CreatedAt is the round creation timestamp when known."
        },
        "updated_at": {
          "type": "string",
          "description": "UpdatedAt is the ledger update timestamp when known."
        },
        "aggregated_at": {
          "type": "string",
          "description": "AggregatedAt is the aggregate timestamp when known."
        },
        "is_active": {
          "type": "boolean",
          "description": "IsActive reports whether local state still points at this round as active."
        },
        "total_assignments": {
          "type": "integer",
          "description": "TotalAssignments is the number of reviewer assignments in the round."
        },
        "submitted_assignments": {
          "type": "integer",
          "description": "SubmittedAssignments is the number of assignments with a submission."
        },
        "pending_assignments": {
          "type": "integer",
          "description": "PendingAssignments is the number of assignments still waiting on a submission."
        },
        "reviewers": {
          "items": {
            "$ref": "#/$defs/ReviewAssignmentView"
          },
          "type": "array",
          "description": "Reviewers lists the reviewer-centric assignment views for the round."
        },
        "blocking_findings": {
          "items": {
            "$ref": "#/$defs/ReviewAggregateFinding"
          },
          "type": "array",
          "description": "BlockingFindings lists aggregate blocking findings when they exist."
        },
        "non_blocking_findings": {
          "items": {
            "$ref": "#/$defs/ReviewAggregateFinding"
          },
          "type": "array",
          "description": "NonBlockingFindings lists aggregate non-blocking findings when they exist."
        },
        "artifacts": {
          "items": {
            "$ref": "#/$defs/ReviewArtifactView"
          },
          "type": "array",
          "description": "Artifacts lists supporting raw review artifacts for the round."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-fatal degraded-state notes for the round."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id"
      ],
      "description": "ReviewRoundView is one read-only UI representation of a review round."
    },
    "ReviewWorklogView": {
      "properties": {
        "review_kind": {
          "type": "string",
          "description": "ReviewKind is the reviewer-reported kind from the coverage payload when available."
        },
        "anchor_sha": {
          "type": "string",
          "description": "AnchorSHA is the reviewer-reported anchor SHA from the coverage payload when available."
        },
        "full_plan_read": {
          "type": "boolean",
          "description": "FullPlanRead reports whether the reviewer marked the full active plan as read."
        },
        "checked_areas": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "CheckedAreas lists the files, checkpoints, or areas the reviewer says it already covered."
        },
        "open_questions": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "OpenQuestions lists unresolved review trails still in flight."
        },
        "candidate_findings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "CandidateFindings lists provisional findings captured during review before they were finalized into the canonical findings payload."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "ReviewWorklogView contains normalized reviewer-progress fields derived from the richer submission payload."
    }
  },
  "title": "Review UI resource",
  "description": "Read-only JSON resource returned by `/api/review` for `harness ui`."
}
