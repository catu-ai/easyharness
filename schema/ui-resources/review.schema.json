{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/ui-resources/review.schema.json",
  "$ref": "#/$defs/ReviewResult",
  "$defs": {
    "ErrorDetail": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path identifies the field, section, or artifact path associated with the error."
        },
        "message": {
          "type": "string",
          "description": "Message is the human-readable explanation of the problem."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "message"
      ],
      "description": "ErrorDetail describes one machine-readable validation or execution problem in a command result."
    },
    "ReviewFindingView": {
      "properties": {
        "finding_id": {
          "type": "string"
        },
        "area": {
          "type": "string"
        },
        "severity": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "details": {
          "type": "string"
        },
        "locations": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "area",
        "severity",
        "title",
        "details"
      ],
      "description": "ReviewFindingView is one decision finding without internal orchestration metadata."
    },
    "ReviewResult": {
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "OK reports whether review loading succeeded."
        },
        "resource": {
          "type": "string",
          "description": "Resource is the stable UI resource identifier."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise human-readable explanation of the loaded review rounds."
        },
        "rounds": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/ReviewRoundView"
              },
              "type": "array",
              "description": "Rounds lists the discovered review rounds for the current plan."
            },
            {
              "type": "null"
            }
          ],
          "description": "Rounds lists the discovered review rounds for the current plan."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-fatal degraded-state notes for the overall review resource."
        },
        "errors": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Errors lists hard failures that prevented review loading."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ok",
        "resource",
        "summary",
        "rounds"
      ],
      "description": "ReviewResult is the read-only UI resource returned by `/api/review`."
    },
    "ReviewReviewerView": {
      "properties": {
        "name": {
          "type": "string",
          "description": "Name is the reviewer-provided identity label when available."
        },
        "status": {
          "type": "string",
          "description": "Status is pending or submitted."
        },
        "submitted_at": {
          "type": "string",
          "description": "SubmittedAt is the submission timestamp when known."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the reviewer's concise assessment."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-fatal degraded-state notes for the reviewer result."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "ReviewReviewerView is the independent reviewer result surfaced for a round. Internal reviewer orchestration is intentionally not part of the UI resource."
    },
    "ReviewRoundView": {
      "properties": {
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "kind": {
          "type": "string",
          "description": "Kind is the review kind for the round."
        },
        "anchor_sha": {
          "type": "string",
          "description": "AnchorSHA is the delta-review anchor commit when one is available."
        },
        "reviewed_head_sha": {
          "type": "string",
          "description": "ReviewedHeadSHA is the immutable candidate commit captured for this round."
        },
        "repairs_round_id": {
          "type": "string",
          "description": "RepairsRoundID is the direct parent coverage round for a repair delta."
        },
        "repair_finding_ids": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "RepairFindingIDs lists the parent findings explicitly targeted by this repair delta."
        },
        "step": {
          "type": "integer",
          "description": "Step is the tracked plan step number when the round is step-scoped."
        },
        "revision": {
          "type": "integer",
          "description": "Revision is the plan-local revision associated with the round."
        },
        "review_title": {
          "type": "string",
          "description": "ReviewTitle is the human-readable title for the round when one exists."
        },
        "status": {
          "type": "string",
          "description": "Status is the stable UI status label for the round."
        },
        "status_summary": {
          "type": "string",
          "description": "StatusSummary is the concise explanation of the current round status."
        },
        "decision": {
          "type": "string",
          "description": "Decision is the completed review decision when one is known."
        },
        "created_at": {
          "type": "string",
          "description": "CreatedAt is the round creation timestamp when known."
        },
        "updated_at": {
          "type": "string",
          "description": "UpdatedAt is the ledger update timestamp when known."
        },
        "decided_at": {
          "type": "string",
          "description": "DecidedAt is the completed review timestamp when known."
        },
        "is_active": {
          "type": "boolean",
          "description": "IsActive reports whether local state still points at this round as active."
        },
        "reviewer": {
          "$ref": "#/$defs/ReviewReviewerView",
          "description": "Reviewer is the independent integrated reviewer shown for this round."
        },
        "blocking_findings": {
          "items": {
            "$ref": "#/$defs/ReviewFindingView"
          },
          "type": "array",
          "description": "BlockingFindings lists the cumulative unresolved blocking findings at this coverage-chain tip, not only findings newly raised by this round."
        },
        "non_blocking_findings": {
          "items": {
            "$ref": "#/$defs/ReviewFindingView"
          },
          "type": "array",
          "description": "NonBlockingFindings lists non-blocking findings when they exist."
        },
        "resolved_finding_ids": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "ResolvedFindingIDs lists repair findings closed by this round."
        },
        "unresolved_finding_ids": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "UnresolvedFindingIDs lists all blocking findings still open at this coverage-chain tip."
        },
        "coverage_status": {
          "type": "string",
          "description": "CoverageStatus is clean, blocked, or pending according to the completed coverage-chain state surfaced for this round."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-fatal degraded-state notes for the round."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id"
      ],
      "description": "ReviewRoundView is one read-only UI representation of a review round."
    }
  },
  "title": "Review UI resource",
  "description": "Read-only JSON resource returned by `/api/review` for `harness ui`."
}
