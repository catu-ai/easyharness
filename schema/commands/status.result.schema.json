{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/commands/status.result.schema.json",
  "$ref": "#/$defs/StatusResult",
  "$defs": {
    "ErrorDetail": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path identifies the field, section, or artifact path associated with the error."
        },
        "message": {
          "type": "string",
          "description": "Message is the human-readable explanation of the problem."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "message"
      ],
      "description": "ErrorDetail describes one machine-readable validation or execution problem in a command result."
    },
    "NextAction": {
      "properties": {
        "command": {
          "oneOf": [
            {
              "type": "string",
              "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
            },
            {
              "type": "null"
            }
          ],
          "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
        },
        "description": {
          "type": "string",
          "description": "Description explains the suggested next step in plain language."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "command",
        "description"
      ],
      "description": "NextAction describes one concrete follow-up action that the caller should consider after reading a command result."
    },
    "ReviewSlot": {
      "properties": {
        "name": {
          "type": "string",
          "description": "Name is the review dimension name."
        },
        "slot": {
          "type": "string",
          "description": "Slot is the stable slot identifier."
        },
        "instructions": {
          "type": "string",
          "description": "Instructions is the reviewer prompt for this slot."
        },
        "submission_path": {
          "type": "string",
          "description": "SubmissionPath is the target path for this slot's submission artifact."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "slot",
        "instructions",
        "submission_path"
      ],
      "description": "ReviewSlot describes one surfaced reviewer submission slot."
    },
    "StatusArtifacts": {
      "properties": {
        "project_root": {
          "type": "string",
          "description": "ProjectRoot is the repository root that anchors surfaced repo-facing paths."
        },
        "plan_path": {
          "type": "string",
          "description": "PlanPath is the active, archived, or last-landed plan path relevant to the current status resolution.",
          "examples": [
            "docs/plans/active/2026-03-31-centralize-contract-schemas-and-generated-reference-docs.md"
          ]
        },
        "supplements_path": {
          "type": "string",
          "description": "SupplementsPath is the companion supplements directory for the current plan package when one exists.",
          "examples": [
            "docs/plans/active/supplements/2026-03-31-centralize-contract-schemas-and-generated-reference-docs"
          ]
        },
        "review_round_id": {
          "type": "string",
          "description": "ReviewRoundID is the active review round identifier when review is in flight."
        },
        "review_slots": {
          "items": {
            "$ref": "#/$defs/ReviewSlot"
          },
          "type": "array",
          "description": "ReviewSlots lists the active round's reviewer-owned slot handles when review is in flight."
        },
        "last_landed_at": {
          "type": "string",
          "description": "LastLandedAt is the timestamp of the most recent landed plan."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusArtifacts lists stable paths or identifiers related to the current status result."
    },
    "StatusEvidenceFacts": {
      "properties": {
        "recorded": {
          "$ref": "#/$defs/StatusRecordedEvidence",
          "description": "Recorded is durable local evidence. Recorded evidence is authoritative for workflow-node progression."
        },
        "remote": {
          "$ref": "#/$defs/StatusRemoteEvidence",
          "description": "Remote is a compact read-only projection of live remote facts. It explains drift or refresh opportunities but does not advance workflow state."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusEvidenceFacts groups archived-candidate handoff evidence."
    },
    "StatusFacts": {
      "properties": {
        "current_step": {
          "type": "string",
          "description": "CurrentStep is the first unfinished plan step when one exists."
        },
        "revision": {
          "type": "integer",
          "description": "Revision is the current plan-local revision number."
        },
        "reopen_mode": {
          "type": "string",
          "description": "ReopenMode is the active reopen mode when a reopen repair path is in effect."
        },
        "review_kind": {
          "type": "string",
          "description": "ReviewKind is the review kind for the active review round."
        },
        "review_trigger": {
          "type": "string",
          "description": "ReviewTrigger is the derived reason label for the active review round."
        },
        "review_title": {
          "type": "string",
          "description": "ReviewTitle is the human-readable title for the active review round when one exists."
        },
        "review_status": {
          "type": "string",
          "description": "ReviewStatus summarizes the aggregate review state for the current node."
        },
        "archive_blocker_count": {
          "type": "integer",
          "description": "ArchiveBlockerCount reports how many archive-readiness blockers are still present."
        },
        "evidence": {
          "$ref": "#/$defs/StatusEvidenceFacts",
          "description": "Evidence groups durable recorded evidence and compact read-only remote observation facts for archived-candidate handoff."
        },
        "land_pr_url": {
          "type": "string",
          "description": "LandPRURL is the pull request URL recorded for the land phase."
        },
        "land_commit": {
          "type": "string",
          "description": "LandCommit is the merge commit or landed commit recorded for the land phase."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusFacts carries selected derived details that help interpret a status node without dumping the full local state model."
    },
    "StatusRecordedEvidence": {
      "properties": {
        "publish": {
          "$ref": "#/$defs/StatusRecordedPublishEvidence",
          "description": "Publish is the latest recorded publish evidence when present."
        },
        "ci": {
          "$ref": "#/$defs/StatusRecordedEvidenceStatus",
          "description": "CI is the latest recorded CI evidence when present."
        },
        "sync": {
          "$ref": "#/$defs/StatusRecordedEvidenceStatus",
          "description": "Sync is the latest recorded sync evidence when present."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusRecordedEvidence summarizes durable archived-candidate evidence."
    },
    "StatusRecordedEvidenceStatus": {
      "properties": {
        "status": {
          "type": "string",
          "description": "Status is the recorded evidence status for this domain."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusRecordedEvidenceStatus summarizes one recorded evidence domain."
    },
    "StatusRecordedPublishEvidence": {
      "properties": {
        "status": {
          "type": "string",
          "description": "Status is the recorded publish evidence status."
        },
        "pr_url": {
          "type": "string",
          "description": "PRURL is the recorded pull request URL when one is available."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusRecordedPublishEvidence summarizes recorded publish evidence."
    },
    "StatusRemoteDegradation": {
      "properties": {
        "code": {
          "type": "string",
          "description": "Code is a stable degradation code such as gh_missing or pr_unreadable."
        },
        "message": {
          "type": "string",
          "description": "Message is a concise human-readable explanation."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "code"
      ],
      "description": "StatusRemoteDegradation explains a non-fatal live remote observation failure."
    },
    "StatusRemoteEvidence": {
      "properties": {
        "observation": {
          "type": "string",
          "description": "Observation describes how complete the live remote observation was, such as complete, partial, or unavailable."
        },
        "assessment": {
          "type": "string",
          "description": "Assessment describes the workflow meaning of the remote facts relative to durable recorded evidence. Commands remain exclusively in next_actions."
        },
        "message": {
          "type": "string",
          "description": "Message explains the remote evidence relationship in concise human-readable language."
        },
        "pr": {
          "$ref": "#/$defs/StatusRemotePRSummary",
          "description": "PR summarizes only the high-signal pull request state needed for workflow guidance."
        },
        "ci": {
          "$ref": "#/$defs/StatusRemoteEvidenceStatus",
          "description": "CI reports the CI evidence status that refresh would record when it can be observed."
        },
        "sync": {
          "$ref": "#/$defs/StatusRemoteEvidenceStatus",
          "description": "Sync reports the sync evidence status that refresh would record when it can be observed."
        },
        "degraded": {
          "items": {
            "$ref": "#/$defs/StatusRemoteDegradation"
          },
          "type": "array",
          "description": "Degraded lists compact non-fatal remote observation failures."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusRemoteEvidence is the compact live, read-only remote state surfaced by `harness status` for an archived candidate with recorded publish PR evidence."
    },
    "StatusRemoteEvidenceStatus": {
      "properties": {
        "status": {
          "type": "string",
          "description": "Status is the evidence status that refresh would record."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "description": "StatusRemoteEvidenceStatus reports the evidence status that a refresh would record for one remote-observed domain."
    },
    "StatusRemotePRSummary": {
      "properties": {
        "state": {
          "type": "string",
          "description": "State is the provider PR state such as OPEN, CLOSED, or MERGED."
        },
        "draft": {
          "type": "boolean",
          "description": "Draft reports whether the observed pull request is a draft."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "draft"
      ],
      "description": "StatusRemotePRSummary summarizes only remote pull request state that can affect controller guidance."
    },
    "StatusResult": {
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "OK reports whether the command succeeded."
        },
        "command": {
          "type": "string",
          "description": "Command is the stable command identifier for the result payload."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise human-readable explanation of the current state."
        },
        "state": {
          "$ref": "#/$defs/StatusState",
          "description": "State is the canonical workflow position resolved for the current worktree."
        },
        "facts": {
          "$ref": "#/$defs/StatusFacts",
          "description": "Facts carries selected high-signal derived details that help explain the current node."
        },
        "artifacts": {
          "$ref": "#/$defs/StatusArtifacts",
          "description": "Artifacts points to stable paths or identifiers that help the caller locate related contract artifacts."
        },
        "next_actions": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/NextAction"
              },
              "type": "array",
              "description": "NextAction lists the most relevant follow-up steps in priority order."
            },
            {
              "type": "null"
            }
          ],
          "description": "NextAction lists the most relevant follow-up steps in priority order."
        },
        "blockers": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Blockers lists state issues that block ordinary execution progression."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-blocking workflow reminders or ambiguity notes."
        },
        "errors": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Errors lists hard failures that prevented full state resolution."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ok",
        "command",
        "summary",
        "state",
        "next_actions"
      ],
      "description": "StatusResult is the JSON result returned by `harness status`."
    },
    "StatusState": {
      "properties": {
        "current_node": {
          "type": "string",
          "description": "CurrentNode is the resolved v0.2 workflow node for the current worktree.",
          "examples": [
            "execution/step-1/implement"
          ]
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "current_node"
      ],
      "description": "StatusState is the canonical workflow node returned by `harness status`."
    }
  },
  "title": "Status command result",
  "description": "JSON output for `harness status`."
}
