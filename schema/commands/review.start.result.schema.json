{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/commands/review.start.result.schema.json",
  "$ref": "#/$defs/ReviewStartResult",
  "$defs": {
    "ErrorDetail": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path identifies the field, section, or artifact path associated with the error."
        },
        "message": {
          "type": "string",
          "description": "Message is the human-readable explanation of the problem."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "message"
      ],
      "description": "ErrorDetail describes one machine-readable validation or execution problem in a command result."
    },
    "NextAction": {
      "properties": {
        "command": {
          "oneOf": [
            {
              "type": "string",
              "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
            },
            {
              "type": "null"
            }
          ],
          "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
        },
        "description": {
          "type": "string",
          "description": "Description explains the suggested next step in plain language."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "command",
        "description"
      ],
      "description": "NextAction describes one concrete follow-up action that the caller should consider after reading a command result."
    },
    "ReviewAssignment": {
      "properties": {
        "slot": {
          "type": "string"
        },
        "role": {
          "type": "string"
        },
        "dimensions": {
          "items": {
            "$ref": "#/$defs/ReviewResolvedDimension"
          },
          "type": "array",
          "minItems": 1
        },
        "instructions": {
          "type": "string"
        },
        "risk_brief": {
          "$ref": "#/$defs/ReviewRiskBrief"
        },
        "submission_path": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "slot",
        "role",
        "dimensions",
        "instructions",
        "submission_path"
      ],
      "description": "ReviewAssignment describes one surfaced reviewer assignment."
    },
    "ReviewResolvedDimension": {
      "properties": {
        "name": {
          "type": "string"
        },
        "sources": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "description": {
          "type": "string"
        },
        "instructions": {
          "type": "string"
        },
        "plan_path": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "sources",
        "description",
        "instructions"
      ],
      "description": "ReviewResolvedDimension snapshots one explicitly assigned guidance fragment."
    },
    "ReviewRiskBrief": {
      "properties": {
        "risk_surfaces": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "invariants": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 1
        },
        "failure_modes": {
          "oneOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "risk_surfaces",
        "invariants"
      ],
      "description": "ReviewRiskBrief gives a specialist a concrete risk surface to challenge."
    },
    "ReviewStartArtifacts": {
      "properties": {
        "project_root": {
          "type": "string",
          "description": "ProjectRoot is the repository root that anchors surfaced repo-facing paths."
        },
        "plan_path": {
          "type": "string",
          "description": "PlanPath is the current plan path associated with the review round."
        },
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "assignments": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/ReviewAssignment"
              },
              "type": "array",
              "description": "Assignments lists the materialized reviewer assignments created for the round."
            },
            {
              "type": "null"
            }
          ],
          "description": "Assignments lists the materialized reviewer assignments created for the round."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "project_root",
        "plan_path",
        "round_id",
        "assignments"
      ],
      "description": "ReviewStartArtifacts lists the review artifacts created by `harness review start`."
    },
    "ReviewStartResult": {
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "OK reports whether the command succeeded."
        },
        "command": {
          "type": "string",
          "description": "Command is the stable command identifier for the result payload."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise human-readable outcome description."
        },
        "artifacts": {
          "$ref": "#/$defs/ReviewStartArtifacts",
          "description": "Artifacts points to the created review artifacts for the round."
        },
        "next_actions": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/NextAction"
              },
              "type": "array",
              "description": "NextAction lists the most relevant follow-up steps in priority order."
            },
            {
              "type": "null"
            }
          ],
          "description": "NextAction lists the most relevant follow-up steps in priority order."
        },
        "errors": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Errors lists hard failures that prevented the command from succeeding."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ok",
        "command",
        "summary",
        "next_actions"
      ],
      "description": "ReviewStartResult is the JSON result returned by `harness review start`."
    }
  },
  "title": "Review start command result",
  "description": "JSON output for `harness review start`."
}
