{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/commands/review.submit.result.schema.json",
  "$ref": "#/$defs/ReviewSubmitResult",
  "$defs": {
    "ErrorDetail": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path identifies the field, section, or artifact path associated with the error."
        },
        "message": {
          "type": "string",
          "description": "Message is the human-readable explanation of the problem."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "message"
      ],
      "description": "ErrorDetail describes one machine-readable validation or execution problem in a command result."
    },
    "NextAction": {
      "properties": {
        "command": {
          "oneOf": [
            {
              "type": "string",
              "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
            },
            {
              "type": "null"
            }
          ],
          "description": "Command is the suggested command line to run next when the next step is best expressed as a harness command."
        },
        "description": {
          "type": "string",
          "description": "Description explains the suggested next step in plain language."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "command",
        "description"
      ],
      "description": "NextAction describes one concrete follow-up action that the caller should consider after reading a command result."
    },
    "ReviewAggregate": {
      "properties": {
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "kind": {
          "type": "string",
          "description": "Kind is the review kind for the round."
        },
        "step": {
          "type": "integer",
          "description": "Step is the tracked plan step number when the round is step-scoped."
        },
        "revision": {
          "type": "integer",
          "description": "Revision is the plan-local revision associated with the round."
        },
        "review_title": {
          "type": "string",
          "description": "ReviewTitle is the human-readable title for the round when one exists."
        },
        "reviewed_head_sha": {
          "type": "string",
          "description": "ReviewedHeadSHA repeats the manifest candidate boundary accepted at aggregation time."
        },
        "repair": {
          "$ref": "#/$defs/ReviewRepairReference"
        },
        "decision": {
          "type": "string",
          "description": "Decision is the aggregate review decision for the round."
        },
        "blocking_findings": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/ReviewAggregateFinding"
              },
              "type": "array",
              "description": "BlockingFindings lists the findings that currently block progression."
            },
            {
              "type": "null"
            }
          ],
          "description": "BlockingFindings lists the findings that currently block progression."
        },
        "non_blocking_findings": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/ReviewAggregateFinding"
              },
              "type": "array",
              "description": "NonBlockingFindings lists the findings that were recorded without blocking progression."
            },
            {
              "type": "null"
            }
          ],
          "description": "NonBlockingFindings lists the findings that were recorded without blocking progression."
        },
        "resolved_finding_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unresolved_finding_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unresolved_blocking_findings": {
          "items": {
            "$ref": "#/$defs/ReviewAggregateFinding"
          },
          "type": "array",
          "description": "UnresolvedBlockingFindings is the cumulative blocking set at this chain tip, including inherited findings not resolved by this round."
        },
        "aggregated_at": {
          "type": "string",
          "description": "AggregatedAt is the aggregate timestamp."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id",
        "kind",
        "revision",
        "reviewed_head_sha",
        "decision",
        "blocking_findings",
        "non_blocking_findings",
        "resolved_finding_ids",
        "unresolved_finding_ids",
        "unresolved_blocking_findings",
        "aggregated_at"
      ],
      "description": "ReviewAggregate is the command-owned aggregate artifact for a completed review round."
    },
    "ReviewAggregateFinding": {
      "properties": {
        "finding_id": {
          "type": "string"
        },
        "slot": {
          "type": "string",
          "description": "Slot is the stable reviewer slot identifier."
        },
        "role": {
          "type": "string"
        },
        "area": {
          "type": "string"
        },
        "severity": {
          "type": "string",
          "description": "Severity is the finding severity label."
        },
        "title": {
          "type": "string",
          "description": "Title is the short human-readable title of the finding."
        },
        "details": {
          "type": "string",
          "description": "Details is the full review finding explanation."
        },
        "locations": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Locations optionally lists lightweight repo-relative source anchors for the finding, such as \"path/to/file.go\", \"path/to/file.go#L123\", or \"path/to/file.go#L1-L3\"."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "finding_id",
        "slot",
        "role",
        "area",
        "severity",
        "title",
        "details"
      ],
      "description": "ReviewAggregateFinding is one aggregate finding annotated with its stable identity and reviewer-assignment provenance."
    },
    "ReviewRepairReference": {
      "properties": {
        "round_id": {
          "type": "string"
        },
        "finding_ids": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "minItems": 0
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "round_id",
        "finding_ids"
      ],
      "description": "ReviewRepairReference identifies the findings targeted by a repair delta."
    },
    "ReviewSubmitArtifacts": {
      "properties": {
        "project_root": {
          "type": "string",
          "description": "ProjectRoot is the repository root that anchors surfaced repo-facing paths."
        },
        "round_id": {
          "type": "string",
          "description": "RoundID is the stable identifier for the review round."
        },
        "submission_path": {
          "type": "string",
          "description": "SubmissionPath is the path to the created submission artifact."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "project_root",
        "round_id",
        "submission_path"
      ],
      "description": "ReviewSubmitArtifacts lists the artifacts touched by `harness review submit`."
    },
    "ReviewSubmitResult": {
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "OK reports whether the command succeeded."
        },
        "command": {
          "type": "string",
          "description": "Command is the stable command identifier for the result payload."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise human-readable outcome description."
        },
        "artifacts": {
          "$ref": "#/$defs/ReviewSubmitArtifacts",
          "description": "Artifacts points to the created submission artifacts."
        },
        "review": {
          "$ref": "#/$defs/ReviewAggregate",
          "description": "Review is the completed review decision derived from the sole submission."
        },
        "next_actions": {
          "oneOf": [
            {
              "items": {
                "$ref": "#/$defs/NextAction"
              },
              "type": "array",
              "description": "NextAction lists the most relevant follow-up steps in priority order."
            },
            {
              "type": "null"
            }
          ],
          "description": "NextAction lists the most relevant follow-up steps in priority order."
        },
        "errors": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Errors lists hard failures that prevented the command from succeeding."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ok",
        "command",
        "summary",
        "next_actions"
      ],
      "description": "ReviewSubmitResult is the JSON result returned by `harness review submit`."
    }
  },
  "title": "Review submit command result",
  "description": "JSON output for `harness review submit`."
}
