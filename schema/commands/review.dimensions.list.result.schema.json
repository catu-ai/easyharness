{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/catu-ai/easyharness/tree/main/schema/commands/review.dimensions.list.result.schema.json",
  "$ref": "#/$defs/ReviewDimensionsListResult",
  "$defs": {
    "ErrorDetail": {
      "properties": {
        "path": {
          "type": "string",
          "description": "Path identifies the field, section, or artifact path associated with the error."
        },
        "message": {
          "type": "string",
          "description": "Message is the human-readable explanation of the problem."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "path",
        "message"
      ],
      "description": "ErrorDetail describes one machine-readable validation or execution problem in a command result."
    },
    "ReviewDimensionMetadata": {
      "properties": {
        "name": {
          "type": "string",
          "description": "Name is the stable review dimension identifier."
        },
        "source": {
          "type": "string",
          "description": "Source identifies where the dimension definition came from."
        },
        "description": {
          "type": "string",
          "description": "Description is the concise selection guidance for controller agents."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "name",
        "source",
        "description"
      ],
      "description": "ReviewDimensionMetadata describes one dimension in the controller-facing catalog."
    },
    "ReviewDimensionsListResult": {
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "OK reports whether the command succeeded."
        },
        "command": {
          "type": "string",
          "description": "Command is the stable command identifier for the result payload."
        },
        "summary": {
          "type": "string",
          "description": "Summary is the concise human-readable outcome description."
        },
        "dimensions": {
          "items": {
            "$ref": "#/$defs/ReviewDimensionMetadata"
          },
          "type": "array",
          "description": "Dimensions lists the discovered review dimensions available to controller agents."
        },
        "warnings": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "Warnings lists non-blocking config issues that did not prevent catalog discovery."
        },
        "errors": {
          "items": {
            "$ref": "#/$defs/ErrorDetail"
          },
          "type": "array",
          "description": "Errors lists hard failures that prevented the command from succeeding."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "ok",
        "command",
        "summary",
        "dimensions"
      ],
      "description": "ReviewDimensionsListResult is the JSON result returned by `harness review dimensions list`."
    }
  },
  "title": "Review dimensions list command result",
  "description": "JSON output for `harness review dimensions list`."
}
