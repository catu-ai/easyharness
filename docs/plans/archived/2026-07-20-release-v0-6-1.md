---
template_version: 0.3.0
created_at: "2026-07-20T22:26:40+08:00"
approved_at: "2026-07-20T22:30:44+08:00"
source_type: direct_request
source_refs:
    - Human patch-release decision, 2026-07-20
    - https://github.com/catu-ai/easyharness/pull/301
size: XS
---

# Release easyharness v0.6.1

<!-- If this plan uses supplements/<plan-stem>/, keep the markdown concise,
absorb repository-facing normative content into formal tracked locations before
archive, and record supplement absorption in Closeout. Lightweight plans should
normally avoid supplements. -->

## Goal

Prepare the dedicated `easyharness v0.6.1` patch release candidate so users of
v0.6.0 receive the lightweight `harness land` repair merged in PR #301. The
candidate changes only the repository version and tracked release plan, proves
the existing VERSION-driven release path, and publishes a narrow PR for
separate merge approval.

After that release PR is approved and landed, verify the generated tag, GitHub
Release assets, checksums, binary metadata, and Homebrew publication before
completing post-merge bookkeeping.

### Decisions and Constraints

- This is a patch release for a v0.6.0 workflow-blocking product bug, not a new
  minor promise or prerelease.
- `VERSION` stores the bare value `0.6.1`; automation is expected to create the
  stable tag `v0.6.1` and a non-prerelease GitHub Release.
- The release candidate contains only the version transition and tracked plan
  lifecycle changes. Product, workflow, schema, UI, and release-automation
  changes are excluded.
- Run the full release-ready validation profile and local packaging check from
  the fixed candidate before publish.
- Stop at `await_merge` for explicit human approval. Tag, asset, and Homebrew
  verification belongs to post-merge `harness-land` bookkeeping.

## Scope

### In Scope

- Set the repository's bare version from `0.6.0` to `0.6.1`.
- Run the complete release-ready validation and local `v0.6.1` packaging
  verification.
- Independently review, archive, publish, and record current PR/CI/sync
  evidence through `await_merge`.
- After later merge approval, verify VERSION-driven release automation,
  platform archives, `SHA256SUMS`, stable Homebrew formula publication, and
  released binary metadata before completing land.

### Out of Scope

- Any additional product, documentation, workflow, prompt, schema, UI, or
  release-automation change.
- A `v0.7.0` milestone or release promise.
- Rewriting or republishing v0.6.0 artifacts.

## Acceptance Criteria

- [x] The candidate contains the exact bare version `0.6.1` with no unrelated
      product or release-automation change.
- [x] `scripts/validate-release` and a local `v0.6.1` packaging check pass,
      including installer, archive, checksums, platform set, and binary version
      metadata.

## Review Focus

- Confirm `0.6.1` is monotonic from `0.6.0`, is classified as a stable patch,
  and maps to tag `v0.6.1`.
- Confirm the candidate is a dedicated release diff and does not bundle
  unrelated behavior or release-automation changes.
- Confirm validation exercises the artifacts users will receive, not only
  ordinary unit tests.
- Confirm the separate merge-approval boundary and post-merge publication
  verification remain intact.

## Deferred Items

- None.

## Work Breakdown

### Step 1: Prepare the v0.6.1 release candidate

- Done: [x]
- Outcome: The repository identifies itself as stable `0.6.1`, the dedicated
  patch candidate passes the release-ready validation profile, and locally
  built artifacts match the intended stable release namespace and metadata.
- Covers: All acceptance criteria.
- Check: Run `scripts/validate-release`, build `v0.6.1` release artifacts, and
  verify the final diff remains release-only.

## Validation Strategy

- Run `scripts/validate-release` from the fixed candidate.
- Build local artifacts with `scripts/build-release --version v0.6.1` and use
  the repository release verifier to check all platforms, checksums, namespace,
  and embedded version metadata.
- Inspect the final diff and candidate history to confirm the release remains
  a dedicated VERSION transition based on current `main`.
- After separate merge approval, require successful tag/release workflows,
  four platform archives plus `SHA256SUMS`, stable Homebrew publication, and
  matching released binary metadata before completing land.

## Closeout

- Archived At: 2026-07-20T22:42:55+08:00
- Revision: 1
- Validation: `scripts/validate-release` passed. Exact `v0.6.1` local packaging
  produced four platform ZIPs with passing `SHA256SUMS`; the darwin/arm64
  binary reported `v0.6.1`, release mode, and the fixed candidate commit.
- Review: Integrated `review-001-full` passed at `033168e` with no findings
  after independently repeating release semantics, smoke, packaging,
  checksum, archive-content, and binary metadata checks.
- Delivered: The dedicated candidate changes the bare repository version from
  `0.6.0` to stable patch `0.6.1` without product or release-automation changes.
- Not Delivered: Tag creation, GitHub Release assets, Homebrew publication,
  merge, and post-merge verification remain behind separate merge approval.
- Follow-Up Issues: NONE
